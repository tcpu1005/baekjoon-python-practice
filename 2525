a,b=input().split()
A=int(a)
B=int(b)
c=input()
C=int(c)
sum=B+C
"""
while문이 아닌 일반 연산자를 이용해서
문제를 풀 수 있다. 기초 중요!
1시간=60분
현재 시각의 분에 걸리는 시간의 분을 더해서
sum으로 표현하고 sum을 60분으로 나누어
몫만 빼면 시간이 된다.
sum에서 나머지가 분이된다.
"""
if(sum>=60):
  A+=(sum/60)
  sum=(sum%60)
  """
A는 시각이므로 24시간이 넘으면 0에서 부터
다시 시작하므로 넘은 시간에서 24를 빼주어
표준 시간 표현법으로 변화시킨다.
  """
if(A>=24):
  A=A-24
#소수점형태로 나오길래 정수형으로 변환
A=int(A)

print(f"{A} {sum}")
